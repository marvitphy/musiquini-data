const classifiers = require('./dist/core');
const features = require('./dist/features');
const formats = require('./dist/formats');
const utils = require('./dist/utils');


module.exports = {
    classifiers,
    features,
    formats,
    utils
}